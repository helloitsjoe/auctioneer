import * as React from 'react';

const style = {
    width: '100%',
    display: 'block',
    height: '100px',
}

export default class AdminHeader extends React.Component {
    render() {
        return (
            <div style={style}>
            </div>
        )
    }
}