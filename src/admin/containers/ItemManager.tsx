import * as React from 'react';

const style = {
    width: '80%',
    height: '500px',
    display: 'inline-block',
    backgroundColor: 'lightgreen',
}

export const ItemManager = () => (
    <div style={style}>
    </div>
)