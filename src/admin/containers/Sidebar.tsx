import * as React from 'react';

const style = {
    width: '20%',
    height: '500px',
    display: 'inline-block',
    backgroundColor: 'lightblue',
}

export const Sidebar = () => (
    <div style={style}>
    </div>
)
