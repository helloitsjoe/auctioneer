import * as React from 'react';

export class BidItem extends React.Component<any, any> {
    render() {
        return(
            <div></div>
            // Bid item title
            // Amount
            // Tap for description/photos
            // Cancel button?
        )
    }
}
